# Comma-separated list of allowed CORS origins for the deployed frontend.
# Falls back to http://localhost:5173 (local dev) in backend/app/main.py if unset.
ALLOWED_ORIGINS=https://your-frontend.vercel.app
